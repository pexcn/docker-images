# netspeeder for docker

## Usage

Available environment variables:
```bash
```

```bash
docker run -d \
  --name netspeeder \
  --restart always \
  --net host \
  --privileged \
  pexcn/docker-images:netspeeder <args>
```
